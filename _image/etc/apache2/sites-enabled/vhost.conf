ServerName localhost

<VirtualHost *:80>
    DocumentRoot /var/www/html
    SetEnv site_name ${HTTPD_CONF_SERVERNAME}

    <Directory /var/www/html/>
        Options -Indexes
        AllowOverride All
        Order Allow,Deny
        Allow from All
    </Directory>
</VirtualHost>
